{
  "buildCommand": "cd hyperdata_terminal && npm install && npm run build",
  "outputDirectory": "hyperdata_terminal/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}